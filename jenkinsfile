pipeline {
    agent any
    stages {
        stage('Run Docker') {
            steps {
                script {
                    docker.image('node:16-alpine').inside('-w "/workspace"') {
                        sh 'echo "Running inside container"'
                    }
                }
            }
        }
    }
}
