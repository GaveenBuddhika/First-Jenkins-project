pipeline {
  agent {
   docker.image('node:16-alpine').inside('-v /c/ProgramData/Jenkins/.jenkins/workspace/first-jenkins-job:/app -w /app') {
    sh 'node --version'
}

  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
  }
}