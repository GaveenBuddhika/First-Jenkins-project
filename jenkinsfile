pipeline {
    agent {
        docker {
            image 'node:16-alpine'
            args '-w /workspace -v /workspace:/workspace'
        }
    }
    stages {
        stage('Build') {
            steps {
                script {
                    sh 'node --version'
                }
            }
        }
    }
}
