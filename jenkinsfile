pipeline {
    agent any
    stages {
        stage('Run Docker') {
            steps {
                script {
                    docker.image('node:16-alpine').inside("-v ${WORKSPACE}:/workspace -w /workspace") {
                        sh 'ls -la'
                    }
                }
            }
        }
    }
}
